<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="110dp"
    android:layout_margin="4dp"
    android:layout_marginBottom="8dp"
    android:background="@drawable/border_style"
    android:gravity="center_vertical"
    android:padding="12dp">

    <!-- Contenitore verticale per SiteID, Communication e Timestamp -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="center_vertical"
        android:layout_marginEnd="8dp"
        android:orientation="vertical">

        <!-- SiteID Text -->
        <TextView
            android:id="@+id/siteID_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="SiteID: 1000"
            android:textColor="#2C2C2C"
            android:textSize="12sp" />

        <!-- ScrollView for Communication Text -->
        <ScrollView
            android:id="@+id/scrollView2"
            android:layout_width="256dp"
            android:layout_height="40dp"
            android:layout_marginTop="4dp">

            <TextView
                android:id="@+id/communication_text_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#2C2C2C"
                android:textSize="16sp" />
        </ScrollView>

        <!-- Timestamp Text -->
        <TextView
            android:id="@+id/timestamp_text_view"
            android:layout_width="95dp"
            android:layout_height="15dp"
            android:layout_gravity="center"
            android:layout_marginTop="4dp"
            android:textColor="#808080"
            android:textSize="10sp" />
    </LinearLayout>

    <!-- Pulsante Thumbs Up -->
    <Button
        android:id="@+id/thumbs_up_icon"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_gravity="center"
        android:background="@drawable/thumb_up_foreground"
        android:scaleX="1.5"
        android:scaleY="1.5"
        android:text=""
        android:textColor="@android:color/white" />

    <!-- View Count -->
    <TextView
        android:id="@+id/view_count_text_view"
        android:layout_width="27dp"
        android:layout_height="40dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="13dp"
        android:textAlignment="center"
        android:textColor="#2C2C2C"
        android:textSize="20sp" />
</LinearLayout>
